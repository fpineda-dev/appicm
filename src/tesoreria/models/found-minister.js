export class Minister {
    /**
     * 
     * @param {like<Minister> dataMinister}
     */

    constructor({id, photo, paragraph, block, footer}){
        this.id = id,
        this.photo = photo,
        this.paragraph = paragraph,
        this.block = block,
        this.footer = footer
    }
}